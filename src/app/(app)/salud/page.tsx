import { SectionPlaceholder } from "@/components/section-placeholder";

export default function SaludPage() {
  return (
    <SectionPlaceholder title="Salud" phase="la Fase 3">
      Importación de Apple Health (CSV y endpoint), últimos días y
      export/import.
    </SectionPlaceholder>
  );
}
