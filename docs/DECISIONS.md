# DECISIONS.md — Registro de decisiones

Cada decisión no cubierta por las specs se resuelve con **lo más simple** y se
anota aquí. Fuente de verdad ante ambigüedad: el PRD (`01-PRD.md`).

Formato: **fecha · decisión · motivo**.

| Fecha | Decisión | Motivo |
|---|---|---|
| 2026-07-13 | **doc 10 · Fase B2 · F-IA-10 importar semana** (PDF/foto/texto) en pantalla Plan, junto a Importar dieta. `fileToAiFile` extraído a `lib/ai-files.ts` (compartido con F-IA-9, sin duplicar). Asignación de sesión→fecha inline (selects sobre los 7 días de la semana), no drag. Verificado en vivo: `TP1_Week_29.pdf`→6 sesiones (T1–T6) con tipo/gasto; running en texto→7 sesiones (agnosticismo) | Punto de entrada elegido por Alex (Plan = «lo que me pautan»). Los 3 modos de entrada porque puede cambiar de programa o preferir escribirlo. Selects > drag: más fiable en móvil (drag = mejora futura). El modelo devuelve `tipo` libre → se normaliza contra el enum en cliente. |
| 2026-07-13 | **doc 10 · Fase B1 · Migración 0003 aditiva** (training_plans, training_sessions, `days.session_ref`). `session_ref` FK con **onDelete: set null** (no cascade); export/restore remapea ids (plans→sessions→days.session_ref); al importar un plan se **cierra el anterior abierto** (valid_to = día previo); `assignSessionsToDays` no pisa días con sesión ya registrada | `set null` porque los días son sagrados (principio 7): borrar un plan no puede borrar el registro del día. La migración solo CREA tablas + 1 columna nullable → 0 pérdidas, reversible con DROP. `valid_to` explícito por semana evita dos planes vigentes a la vez. No pisar días ya registrados = spec B2 ("sin pisar días registrados manualmente"). |
| 2026-07-12 | **Ajustes en 3 grupos (Atleta / App / Cuenta) + perfil aireado** (quick-fix doc 11): NO se añade icono de perfil en el header (se descartó vs agrupar dentro de Ajustes); inputs del perfil subidos a **44px** (antes 32px) y campos en sub-secciones con separador | Alex: «se mezcla» (identidad vs mantenimiento) + «la info está muy compacta». El perfil es destino de uso raro (set-once) → no gana chrome permanente en el header (opción A descartada); la separación se logra con jerarquía dentro de Ajustes (opción B). Los 32px incumplían el target táctil de 05-DISENO §4; 44px lo corrige y de paso airea. |
| 2026-07-12 | **doc 10 · Fase A · Perfil de atleta en setting `athleteProfile` (jsonb, sin migración)** con historial de objetivos; `edad` derivada de `fechaNacimiento` y `diasEntrenoSemana` derivado del mapeo `sessionByWeekday` (NO se guardan). Precarga = los valores hoy hardcodeados; `fechaNacimiento` por defecto `1993-01-01` (deriva 33 años en 2026, editable) | Principio 9: la IA habla con el atleta de hoy, nada a fuego. Merge superficial sobre defaults → campos nuevos futuros sin migración. No sé la fecha real de nacimiento de Alex; se elige una que reproduce los 33 años hardcodeados y es editable en Ajustes. Una sola fuente de verdad para días/semana (el mapeo) evita que perfil y calendario se contradigan. |
| 2026-07-12 | **doc 10 · Fase A · `ATHLETE_CONTEXT` dinámico**: plantilla congelada (04-IA), valores del perfil. Se colapsa `athleteContextExtended` en el completo (la nota clínica pasa a ser campo `notaClinica` del perfil, presente en el contexto completo de coach/WOD/visita/chat) | «La IA sabe todos mis datos siempre» (requisito de Alex): el contexto completo lleva ya la nota clínica y lesiones; no hace falta una variante «extendida» aparte. Sigue siendo interpolación de variables sobre un prompt congelado (contrato de prompts intacto). |
| 2026-07-12 | **doc 10 · Fase A · Contexto compacto + cláusula anti-sesgo en TODAS las estimaciones (F-IA-1/2/3/4/9)**, con excepción de escala solo en foto (F-IA-1). Cada ruta de estimación hace ahora 1 lectura extra de BD (perfil+mapeo+peso) → `serverError` si falla | El perfil es contexto, no debe mover las estimaciones (los macros son del alimento). Verificado con el test del café ×3 (ver fila «café ×3 tras doc 10»). La lectura extra es un settings diminuto co-localizado en `fra1`; coste despreciable. |
| 2026-07-12 | **doc 10 · Fase A · Guardarraíles del coach (F-IA-6)**: anti-suplementación (paridad con el chat, solo suplementos del perfil) + anti-entreno-fantasma; y **Coach/Chat/Visita miran `sessionByWeekday`**: día sin sesión registrada → «según tu calendario semanal, hoy toca: {X}» | Arregla el bug real del 2026-07-12 (domingo/descanso): el coach asumía entreno y recomendaba whey (que Alex no toma). El mapeo ya tenía D→Descanso; solo faltaba que el coach lo mirara y tuviera el guardarraíl del principio 8. |
| 2026-07-12 | **doc 10 · Fase A · café ×3 tras el contexto compacto = IDÉNTICAS** (`gemini-3.1-flash-lite`, thinking low): kcal 70/70/70, P 4,5, C 9,5, G 1,5 (spread 0). La **kcal (cifra que manda) coincide con la referencia** (70); los macros difieren de la fila «AC F2» (P 6,4 · G 0,4), pero un A/B ahora (`scripts/ai-ab-context.ts`, borrado tras usarlo) demostró que es **deriva del modelo, no de mi cambio**: SIN contexto hoy da P 4,8 · G 1,2 y CON contexto P 4,5 · G 1,5 → el contexto solo mueve ~0,3 g (sub-gramo, dentro del ruido que la propia referencia aceptaba). | AC de la Fase A: re-ejecutar el café ×3 y comparar con la fila «AC F2 · Test de consistencia Gemini». Determinismo perfecto (principio 2) + kcal estable + la cláusula anti-sesgo confinando el efecto del perfil a sub-gramo → NO se cruza el umbral de «parar y revisar». |
| 2026-07-12 | **Fase 5 · Región de función fijada a `fra1` (dashboard Vercel) + redeploy** — verificado en vivo `x-vercel-id: cdg1::fra1` en `fuelboard-ten.vercel.app` | Cerraba el riesgo de DECISIONS #57: en Hobby, `preferredRegion` del código se ignoraba y la función corría en `iad1` (Washington), cruzando el Atlántico contra Neon (eu-central-1) en cada carga. El cambio de dashboard solo aplica a deploys nuevos → hizo falta redeploy. Tras él, TTFB `/hoy` autenticado: frío ~0,97 s, caliente ~0,15 s (antes ~1,1 s). **Nota:** `fuelboard.vercel.app` NO es este proyecto (dominio ajeno, daba 404/iad1 y despistó la 1ª medición); el alias real es `fuelboard-ten.vercel.app` / `fuelboard-kappys1s-projects.vercel.app`. |
| 2026-07-12 | **Fase 5 · LCP: se acepta el real (~0,5 s) y se documenta que el 4,2 s de Lighthouse es lab (slow-4G simulado)** — no se persigue el número de laboratorio | Objetivo 06 §Fase5 «LCP <2 s en móvil». Medido: LCP real (CPU 4× + red real) **~0,52 s** ✅; Lighthouse móvil (slow-4G simulado 1,6 Mbps/150 ms RTT/CPU 4×) da **4,2 s** ❌, dominado por el JS de App Router (~150 KiB + prefetch RSC de la bottom-nav) saturando el enlace simulado — el elemento LCP es texto (banner de fase), no cómputo ni región (TTFB lab 90 ms, CLS 0, TBT 30 ms, Perf 85). Bajar la cifra simulada a <2 s exigiría recortes de RSC/JS desproporcionados para un metric que no refleja el uso real de Alex (wifi/4-5G). Palancas anotadas si algún día importa: afinar prefetch, recortar 157 KiB de JS sin usar, preload de fuente del LCP. |
| 2026-07-12 | **Fase 5 · Input de búsqueda universal se queda DENTRO del sheet (capa 1)**, no "arriba de Hoy" | Conflicto 07 §3 ("input arriba de Hoy") vs 09 §4 (input en la capa 1 del sheet). Jerarquía de CLAUDE.md: **09 manda sobre la organización de pantallas** → prevalece 09 §4. Cero tarjetas/inputs permanentes extra en Hoy (09 §6). El sheet abre con teclado activo (`autoFocus`), así que el coste es 1 toque (abrir sheet) + escribir, no una página nueva. |
| 2026-07-12 | **Fase 5 · Cableado peso: shortcut manifest «Peso de hoy» → `WeightExpressSheet` (exprés, solo peso); línea de estado matinal → `CheckinMatinal` (3 pasos)** | 09 §5 y §5b se contradicen sobre qué abre cada disparador. Se reparte para que AMBOS componentes queden vivos y cada spec tenga un disparador: §5b («el shortcut abre directamente el sheet exprés») → shortcut→exprés; §5 («la línea de estado matinal dispara el check-in») → línea→matinal (el check-in de 3 pasos es el que cronometra el AC 09 §7 ≤15 s). Antes el `WeightExpressSheet` era código muerto (`setWeightOpen(true)` no se llamaba nunca). |
| 2026-07-12 | **Fase 4 · PWA con `@serwist/turbopack`** (no `@serwist/next`) + SW `force-static` en `/serwist/[path]`; **`useNativeEsbuild:true`** + `esbuild` como devDep directa y `serverExternalPackages:["esbuild"]` | Next 16 buildea con Turbopack, y `@serwist/next` es webpack-only. `@serwist/turbopack` compila el SW en build time como archivo estático (sin esbuild en runtime). El default macOS es `esbuild-wasm` (no instalado) → se usa el nativo, que además hay que resolver a nivel de app (devDep) y externalizar para que no lo empaquete el bundler. |
| 2026-07-12 | **`pnpm.ignoredBuiltDependencies: ["@swc/core"]`** (dep transitiva de `@serwist/turbopack`) | La config corporativa de pnpm hace fatal `ERR_PNPM_IGNORED_BUILDS`; `@swc/core` trae binarios prebuilt (no necesita su script de build) → se marca ignorado explícito para que el install no aborte (local y Vercel). |
| 2026-07-12 | **F-IA-9: PDF va NATIVO a Gemini** (bloque `type:"file"`, `application/pdf`), sin `pdf-to-img`/`sharp` para rasterizar; imágenes normalizadas (HEIC→JPEG); `maxOutputTokens:8192` (spec decía 3000) | Gemini lee PDFs como documento → menos dependencias y menos superficie de fallo. 3000 era para Claude (sin tokens de thinking); con visión Gemini el desglose de ~34 opciones + thinking los agota (misma clase de bug que DECISIONS #48 en foto). |
| 2026-07-12 | **Chat (F-IA-8): streaming con `streamText().toTextStreamResponse()`** (+ `X-Thread-Id` en header); resumen cacheado del historial en columnas **`chat_threads.summary`/`summary_msg_count`** (migración 0002); ventana 12 verbatim + resumen por lotes de 6 | AI SDK v7 nativo; el header devuelve el id del hilo recién creado sin cortar el stream. El resumen por lotes evita regenerar en cada turno (spec pide "cacheado") sin perder mensajes (los no resumidos viajan verbatim). `temperature:0.3` (única excepción a temp 0, es conversación). |
| 2026-07-12 | **Coach/Chat/Preparar-visita usan `AI_MODEL_COACH`** (kind "coach", thinking por defecto) vía `runText` (texto plano) / `streamText` | 04-IA asigna el modelo COACH a F-IA-6/7 (y el chat es conversacional). **REQUIERE** setear `AI_MODEL_COACH` en `.env.local` y Vercel (p. ej. un Gemini Flash); si falta, las rutas dan error IA VISIBLE ("Falta la variable AI_MODEL_COACH"), no silencio. |
| 2026-07-12 | **MED · difs en `analytics/medDeltas` (puro + test): SIEMPRE `actual − anterior`**; color semántico grasa↓/músculo↑ verde, **peso neutral** | F5.2 (el Excel del nutri trae signos volteados). La fórmula vive en analytics (no en el componente); el peso solo no es "bueno/malo" en recomposición → se muestra con signo pero sin color. Entrada retroactiva con fecha libre para el histórico 2025-26. |
| 2026-07-12 | **Coach ✨ abre sheet** (F-IA-6, modos hoy/ayer) — supera DECISIONS #43 (toast "Fase 4") | Cierre de Fase 4: el acceso inerte pasa a funcional. |
| 2026-07-12 | **Cola offline en IndexedDB (`idb`)** para addEntries y campos del día; replay en orden al reconectar (`OfflineSync`); **share target vía cache del SW** (`/share-target` POST → `caches`→ `/hoy?share=1`) | 07 §2/§3 + AC Fase 4 (avión→registrar→sincroniza; compartir foto→análisis). La UI ya es optimista; la cola solo garantiza que el servidor cuadre (0 pérdidas). Botones IA deshabilitados sin red con motivo (07 §4). |
| 2026-07-12 | **`temperature: 0` TAMBIÉN en Google** (revierte "no fijar temperature en Gemini") | El default de Gemini es 1.0 → alta aleatoriedad: el analizador de WOD (F-IA-5) daba resultados dispares (150 vs 240 min, 950-1250 vs 1400-1800 kcal) para la MISMA entrada, violando el principio 2. Gemini soporta temperature 0 (decodificación voraz). Verificado: 3 llamadas seguidas del WOD real → idénticas (240 min · 1200-1600 kcal). Se aplica a estimación y visión (+ thinkingLevel por tarea). |
| 2026-07-11 | **Backup automático PRE-restore** (descarga el export del estado actual antes de aplicar; si falla, aborta) | Principio 7 (0 pérdidas) + el restore es secuencial no-transaccional (neon-http): un restore a medias jamás puede perder datos porque el estado previo queda ya descargado en el disco del usuario. La descarga se hace en cliente (`fetch /api/export` → blob) antes de la 1ª escritura. |
| 2026-07-11 | **CSV real de HAE gitignored** (`hae-real.csv`, `HealthAutoExport-*.csv`) | Contiene datos de salud personales (incl. actividad sexual); mismo criterio que el export del PoC (commit 7bdec6d). El test condicional lo usa en local (pasa 31 filas/10 métricas/§4.2) y se salta donde no existe. **Delimitador real: coma + decimales con punto** (autodetectado); needle `basal` afinada para no colisionar con «Temperatura Basal del Cuerpo». |
| 2026-07-11 | **Input de foto SIN `capture`** (antes `capture="environment"`) | En móvil `capture` FUERZA la cámara y oculta la galería; el usuario no podía «elegir foto». F8.1 lo mencionaba, pero 09 §4 y F2.8 mandan «cámara/galería» — sin `capture`, iOS/Android muestran el selector nativo (Cámara · Fototeca · Archivos). La cámara sigue disponible; en escritorio no cambia nada. |
| 2026-07-10 | **Auth: iron-session** (no Auth.js) | Usuario único con password en env; lo más simple. Cookie httpOnly cifrada + argon2. |
| 2026-07-10 | **Proxy** (`src/proxy.ts`) en vez de `middleware.ts` | Next 16 renombró middleware→proxy (runtime nodejs). Chequeo optimista de cookie en el proxy + verificación autoritativa (`getSession`) en el layout de `(app)`. |
| 2026-07-10 | **Driver DB: `@neondatabase/serverless` (neon-http)** | Suficiente para usuario único y funciona en Vercel; el seed lo reutiliza. Optimización futura documentada: `pg` + pool + `attachDatabasePool` con Vercel Fluid compute (recomendado por Neon para más carga). |
| 2026-07-10 | `phase` enum incluye `'normal'` y la columna es **nullable** (null = normal) | Reconcilia §1 (enum con normal) y §2 (Normal = null en BD). |
| 2026-07-10 | Merienda «conjunto»: `grp` asignado por tipo de alimento (Pan→Hidratos, Crema cacahuete→Grasa, Mermelada→Otros) | No existe valor de enum `conjunto`; la derivación de merienda suma por `meal`, así que `grp` no interfiere. |
| 2026-07-10 | `favorites` sin `base_g`: gramos/ml en el nombre; `meal` por defecto `almuerzo` | El esquema §1 de favorites no tiene base_g; los 4 favoritos son snacks de mañana. |
| 2026-07-10 | `diet_versions` semilla: `effective_from = '2025-01-01'`, `carb/fat target = null` | Baseline que cubre todas las fechas; §5 solo da kcal (1800) y prot (110); carb/fat se derivan del plan en Fase 1. |
| 2026-07-10 | `.env.local` escapa los `$` del hash argon2 (`\$`); en Vercel el hash va **crudo** | Next carga `.env*` con dotenv-expand (expande `$VAR`); Vercel inyecta el env directamente sin expansión. |
| 2026-07-10 | shadcn init con base **radix** + preset **nova**; colores **sobrescritos** por los tokens de `05-DISENO §2` | Radix = primitivas del spec; el preset solo aporta scaffolding, el tema es nuestro (si parece shadcn de fábrica, está mal). |
| 2026-07-10 | Password de Alex **temporal** generado en Fase 0 | Desbloquea la verificación de login; cambiar con `pnpm hash-password '<nuevo>'` y actualizar `AUTH_PASSWORD_HASH` (local y Vercel). |
| 2026-07-10 | `create-next-app --agents-md` creó `AGENTS.md` (nota Next 16) e importó `@AGENTS.md` al inicio de `CLAUDE.md` | El scaffold sobrescribió el CLAUDE.md original; restaurado íntegro conservando el import útil. |
| 2026-07-10 | Proyecto Vercel **`fuelboard`** (dir `myHealthPlanner` tiene mayúsculas, inválido como nombre) | Nombres de proyecto Vercel deben ir en minúsculas. |
| 2026-07-10 | **Bug de contraste corregido**: `--muted-foreground` resolvía a `--surface-2` (fondo) por reasignar `--muted` tras `var(--muted)`. Ahora `--muted-foreground` = color de texto atenuado directo (#5D6862 / #93A099) | Las CSS vars se resuelven en uso, no en declaración; la colisión de nombre shadcn(`--muted`=fondo) vs spec(`--muted`=texto) dejaba los textos atenuados casi invisibles en ambos temas. |
| 2026-07-10 | **Errores en naranja legible**: `--destructive` = #B8480C (claro) / #F2894C (oscuro), distinto del relleno de macro `--fat` (#E8590C) | #E8590C como TEXTO sobre blanco da 3.3:1 (<4.5 AA). El naranja de macro se mantiene para barras/gráficos; el de texto de error cumple AA. |
| 2026-07-10 | **Ámbar «hidratos» claro** #B8860B → **#B0800A** | El original daba 2.94:1 sobre `--bg` (<3:1 WCAG 1.4.11 para rellenos). Oscurecido lo mínimo para pasar; sigue ámbar. |
| 2026-07-10 | **Auditoría de contraste automatizada** (`src/lib/contrast.ts` + test + `pnpm audit:contrast`) enganchada a `pnpm test` | Los tokens son los cimientos; un contraste roto debe romper el build. Texto ≥4.5:1, rellenos ≥3:1; bordes sutiles por diseño (no bloqueantes). |
| 2026-07-10 | `turbopack.root` = `process.cwd()` (no `import.meta.dirname`) | En el config de Next `import.meta.dirname` resolvía mal (apuntaba a `src/app`) y rompía Turbopack; los comandos de Next corren desde la raíz. |
| 2026-07-10 | Borrado `pnpm-workspace.yaml` autogenerado (placeholders de `allowBuilds`) | pnpm lo crea por la config global `min-release-age`/aprobación de builds; con placeholders inválidos rompe `pnpm run` en pnpm 9. No es un workspace; deps nativas ya compiladas. |
| 2026-07-10 | Login: input con fondo `--surface-2` y altura 44px; botón 44px | Distinguir input de la tarjeta y cumplir target táctil ≥44px (05-DISENO §4). |
| 2026-07-10 | **Nav rehecha a 4 pestañas** (Hoy · Plan · Progreso · Chat) + **Ajustes en el header** (icono); eliminadas Salud/MED/Tendencia como pestañas | Docs actualizados a 00–09: `09-FLUJOS-UX §2` sustituye la organización de pantallas del PRD. Progreso fusiona Tendencia+MED; Salud pasa a Ajustes; Chat es pestaña propia. |
| 2026-07-10 | Tema (ThemeToggle) y logout movidos del header a la pantalla **Ajustes** | 09 §2: el tema y las operaciones de mantenimiento viven en Ajustes; el header solo lleva el wordmark + icono de Ajustes. |
| 2026-07-10 | `CLAUDE.md` regenerado con docs 08/09, regla de jerarquía **09>PRD** en estructura y nav de 4 pestañas | El paquete de specs pasó de 00–07 a 00–09. |
| 2026-07-10 | **Fase 1** · Plan semilla extraído a `src/server/db/plan-seed.ts` (fuente única de `seed.ts` y del test de `planDerived`) | Evita duplicar la tabla del plan; el test de derivados asalta la misma verdad que el seed. |
| 2026-07-10 | **Derivado del plan (seed) exacto**: 1.625,5 kcal medio · rango 1.270-2.013; carb 150 / fat 67 | El AC F1.4 («~1.700-1.800 medio, ~1.550-1.950 rango») es aproximado; el valor exacto del formula §3 sobre el seed queda fijado en el test (pescado azul 380 kcal ensancha el máx). |
| 2026-07-10 | **Migración PoC**: crea `diet_version` propia (`note='migrated:poc'`, `effective_from`=log más antiguo=2026-06-24, targets 1800/110/**215/55**) además del seed | §6 pide versión desde `targets`; se conserva el seed como baseline (2025-01-01). La migrada es la vigente (effective_from más reciente ≤ hoy) → Plan/Hoy leen el plan real del PoC. |
| 2026-07-10 | **Idempotencia de migración por tabla**: `plan_options`/`meal_entries` = delete-de-claves+insert (fidelidad exacta, preserva duplicados legítimos del PoC); `days`/`health_metrics` = upsert por PK; `med` = insert-si-no-existe-tupla-idéntica (no clobbera MED a mano) | Content-dedup rompería las entradas repetidas reales (favs añadidos 2×); delete-by-date da 0 pérdidas y re-run estable. Verificado: 2ª pasada mantiene 33/2/31/16/5 sin acumular. |
| 2026-07-10 | `meal_entries.created_at` derivado del `id` (epoch ms) del PoC | Preserva el ORDEN de las entradas dentro del día en el timeline; determinista → idempotente. |
| 2026-07-10 | **Fase 1 · Datos cliente**: el RSC entrega `getTodayPayload` como `initialData` de TanStack Query (key `['today',date]`); mutaciones **optimistas** + `router.refresh` en Plan | La BD es la fuente de verdad; el día actual llega renderizado (07 §2). Un único payload agregado por pantalla (simple para usuario único). |
| 2026-07-10 | **Versionado F1.5 pragmático en Fase 1**: cambiar OBJETIVOS crea `diet_version` nueva (copiando opciones); editar/crear/borrar OPCIONES es in-place sobre la versión vigente | Copy-on-write por cada edición de opción fragmentaría el plan; el import de dieta completo como versión nueva llega con F-IA-9 (Fase 4). El AC de F1.5 (targets) queda cubierto. |
| 2026-07-10 | **Capas IA del sheet deshabilitadas con badge "Fase 2"** (Foto, Describir); Coach ✨ → toast "Fase 4"; búsqueda sin match → aviso "IA en Fase 2" | 06/08: no adelantar fases; los accesos existen visibles pero inertes. |
| 2026-07-10 | **FuelGauge**: barra de kcal segmentada por comida (cobalto con alfa decreciente; extra en naranja); fase especial → variante azul-info + nota "no cuenta como desviación" (nunca rojo) | 05 §1/§9 + principio 4/F2.10: el gauge no regaña por hacer lo correcto. Verificado con el día Carga del PoC (07-09: +1417 en azul). |
| 2026-07-10 | **Sesión-por-día** en `settings` (jsonb, key `sessionByWeekday`, ISO 1-7); default L→T1…D→Descanso; editor en Ajustes; precarga el check-in matinal | 09 §5: la app no pregunta lo que puede saber. |
| 2026-07-10 | AC Fase 1 verificados en navegador: favoritos **2 toques**, "Del plan" con gramos precargados a `baseG` y reescalado en vivo, gauge optimista + banner "+X" tras añadir, fechas en español | Cookie de sesión sellada a mano para la verificación (password de Alex no disponible); entrada de prueba borrada de la BD tras verificar. |
| 2026-07-10 | **Login robusto**: input controlado, botón deshabilitado hasta escribir, error visible si falla | El warning de hidratación `__gcruniqueid` lo inyecta una extensión del navegador (gestor de contraseñas), no es bug. |
| 2026-07-10 | **`allowedDevOrigins: ["192.168.0.27"]`** en next.config | Next 16 bloquea recursos de dev cross-origin; sin esto el móvil (por IP wifi) NO hidrataba (página muerta). Solo afecta a `next dev`. |
| 2026-07-10 | **Rendimiento de navegación (07 §2)**: `loading.tsx` con skeletons (altos reservados → 0 layout shift) en hoy/plan/progreso/ajustes; `prefetch` en la bottom-nav; `preferredRegion="fra1"` en el layout de `(app)` | Diagnóstico: (1) la espera de *segundos* era **dev (Turbopack compilando por ruta)**; (2) en prod SIN feedback (sin skeleton) → boundary de loading precargado en **~2 ms** = skeleton instantáneo. (3) **La función corría en `iad1` (Washington)** — `x-vercel-id: cdg1::iad1` (edge cdg1, compute iad1) — así que cada query a Neon (eu-central-1) cruzaba el Atlántico: /hoy medido **~0,29-0,71 s** en prod. `fra1` co-localiza la función con la BD. `getTodayPayload` ya paraleliza con `Promise.all`. Nota: en plan Hobby `preferredRegion` puede ignorarse; si `x-vercel-id` sigue en iad1, fijar la región en Vercel → Settings → Functions. |
| 2026-07-10 | **Fase 2 · Stack IA**: `ai@7` + `@ai-sdk/google@4` (+ `@ai-sdk/provider-utils` como dep directa para el tipo `ProviderOptions`) | AI SDK v7 (no v5/v6 de mi memoria); doc leída en `node_modules/ai/docs`. `provider-utils` es transitiva pero pnpm aísla, así que el `import type { ProviderOptions }` exige declararla directa. |
| 2026-07-10 | **Salida estructurada NATIVA (`Output.object`)** en `server/ai/client.ts` (no parseo manual de texto) → con Google fuerza `responseSchema` = JSON válido garantizado contra el schema Zod; se conserva el 1 reintento re-adjuntando el error al prompt | **Corrige un fallo real de Fase 2**: el parseo manual (que asumía 04-IA §6) rompía con Gemini de visión ("JSON inválido en posición 124") — la propia 04-IA (línea 5) pide revalidar la disciplina JSON al cambiar de proveedor. `Output.object` elimina esa clase de error; los prompts siguen literales. **RATIFICADO** (visto bueno de Alex, 2026-07-11 · cierre de Fase 3): el desvío consciente respecto a 04-IA §6 (parseo manual) queda como decisión firme. |
| 2026-07-10 | **Determinismo/tuning por proveedor** en `server/ai/provider.ts`: Google → NO `temperature`; estimación (F-IA-2/3/4/5) `thinkingLevel:"low"`; **visión (F-IA-1) `thinkingLevel:"medium"` + `mediaResolution:HIGH`**; Anthropic/OpenAI → `temperature:0` | Regla de 04-IA. **Desvío consciente**: 04-IA decía "default en visión", pero el default de Gemini 3.5 Flash tardaba ~1,6 min (inviable). "medium"+alta resolución equilibra calidad de estimación de ración y latencia. Aun así, los gramos-desde-foto son intrínsecamente imprecisos sin referencia de escala → la UI ancla `_base` y deja **editar gramos** (recompute proporcional); la foto es punto de partida, no verdad (principios 2 y 3). El coach (Fase 4) mantiene default. **RATIFICADO** (visto bueno de Alex, 2026-07-11 · cierre de Fase 3): visión con `thinkingLevel:"medium"` + `mediaResolution:HIGH` queda como decisión firme frente al "default en visión" de 04-IA. |
| 2026-07-10 | **1 reintento en lecturas de BD de las rutas IA** (`lib/retry.ts`) + separación error-BD (`serverError`, con `.cause` real de Neon) vs error-IA (`aiErrorResponse`) | La 1ª query a Neon tras inactividad (scale-to-zero) falló en `neon-http` (no reintenta) y se etiquetaba como "IA:". El reintento la absorbe; el etiquetado correcto la hace diagnosticable. |
| 2026-07-10 | **Seam agnóstico real pero mínimo**: solo Google cableado; `anthropic`/`openai` lanzan "instala @ai-sdk/X y añade el caso" | Cumple "cambiar env vars reapunta features" sin arrastrar deps muertas. Cambiar de proveedor = 1 install + 1 caso, sin tocar prompts/features (08 prompt de "cambio de proveedor"). |
| 2026-07-10 | **Foto → Blob PRIVADO + servido con sesión** (02 §3.2 cumplido): subida SOLO en `add()`; `put(access:"private", addRandomSuffix:true)`; se guarda el `pathname` como `photo_url=/api/photos/view?p=…`; esa ruta hace `ensureAuth` + `get(pathname,{access:"private"})` y transmite el stream | La store se creó privada (default nuevo de Vercel) → `access:"public"` fallaba. Privado es más seguro y es justo lo que pedía la spec ("acceso solo con sesión"); ya no es deuda. `newEntryZ.photoUrl` deja de exigir URL absoluta (ahora es ruta propia). Fallo de Blob en `add()` NO bloquea el registro (se añade sin miniatura, principios 3 y 7). |
| 2026-07-10 | **HEIC → sharp en servidor** (`normalizeImage`): cliente reduce a ≤1024/JPEG 0.8; HEIC va original y el servidor convierte; límite 8 MB; `serverExternalPackages:["sharp"]` | 02 §3.2 / F-IA-1. Nota: HEIC grande puede rozar el límite de cuerpo de función de Vercel (~4,5 MB base64); caso borde de desktop (iOS ya da JPEG). |
| 2026-07-10 | **Capa "Describir" = F-IA-4** (volcado del día, una comida o el día entero, la IA asigna `comida` por item); **fallback de búsqueda sin match = F-IA-2** (estima 4 macros, editable, `source:"ia"`) | 09 §4 unifica F2.7 + F-IA-2/4 en una puerta; el fallback de búsqueda es F-IA-2 (item único). |
| 2026-07-10 | **Toolchain: pnpm vía `corepack pnpm@11.9.0`** | `node_modules` linkado a store v11; la pnpm 10.8.0 local usa store v10 y la shell wrappea `pnpm/node` con un `_load_nvm` roto. corepack 11.9.0 (cacheado) coincide con la store. |
| 2026-07-10 | **Fase 3 · Detección de columnas HAE tolerante a acentos** (`normalizeKey`: NFD + quita diacríticos + minúsculas) sobre la tabla EXACTA de 03 §4.2 | La spec da substrings sin acentos («cardiaca en reposo»), pero las cabeceras reales de HAE los llevan («Cardíaca»). Normalizar es estrictamente más tolerante y NO rompe la distinción «peso (» vs «paso» (el paréntesis se conserva). Substrings 1:1 con §4.2; no se añaden extra. |
| 2026-07-10 | **CSV HAE: delimitador autodetectado (`;` preferido en ES)** + parser con comillas | Los números españoles usan coma decimal; con `,` de delimitador el CSV sería inparseable → HAE exporta con `;`. Se detecta por conteo en la cabecera (`;`/`,`/tab). `parseNumberEs` tolera miles («1.234,5») además de coma decimal. |
| 2026-07-10 | **Analítica pura importable en cliente** (`server/analytics/{ma7,deficit,adherence,types}`) sin dependencias de BD | La pantalla Tendencia recalcula al cambiar el rango (14/30/90/todo) SIN pedir datos: el RSC entrega la serie diaria una vez (`getTrendData`) y el cliente aplica las fórmulas puras. Fórmulas EXACTAS de 03 §3; tests hand-computed (ma7 excluye especiales +2 días post-competición; déficit 440 kcal en el fixture; adherencia solo Normal). |
| 2026-07-10 | **Precedencia importado>manual (principio 6) en la vista de Tendencia**: `weight/water/%grasa = health_metrics ?? days` | `health_metrics` y `days` van separados a propósito; la fusión con precedencia health se hace al ensamblar `getTrendData` (peso del reloj/CSV machaca el manual cuando existe). La ingesta CSV va a `health_metrics` (source csv/endpoint), no pisa físicamente `days`. |
| 2026-07-10 | **`applyHealthDays` = upsert con FUSIÓN por campo** (no reemplazo de fila) | Una Automation de HAE que solo trae `steps` no debe borrar el `weight` previo del mismo día. Se insertan/actualizan solo los campos presentes; `updatedAt` siempre. |
| 2026-07-10 | **Import CSV y restore con VISTA PREVIA antes de aplicar** (07 §4); restore CONFIRMA (excepción a «undo, no confirmaciones») | 07 §4 exige resumen (filas, métricas, kJ→kcal, «N días machacan manual») antes de tocar datos. El restore es destructivo (reemplaza todo) → `window.confirm` explícito (07 §2 lista restore entre las excepciones que sí confirman). |
| 2026-07-10 | **Restore = delete-all + insert con remapeo de ids** (diet_version→plan_options, chat_thread→chat_message); secuencial sin transacción | neon-http no soporta transacciones interactivas; el restore es una operación manual sobre un backup propio (usuario único), mismo patrón no-transaccional que migrate:poc. Las tablas de identidad se reinsertan sin id (Postgres reasigna) y las FKs se remapean por el id antiguo del archivo. |
| 2026-07-10 | **Recharts 3.9.2** para los gráficos; colores vía tokens (`stroke/fill="var(--primary)"`), tooltip tematizado | SVG resuelve `var()` en atributos de presentación → gráficos tema-conscientes sin JS. Tooltip custom con fondo `--surface` (05 §5: nada de gridlines agresivas ni look de shadcn de fábrica). Barras de días en fase especial atenuadas (contexto, no desviación — principio 4). |
| 2026-07-10 | **Popover «cómo se calcula» derivado en servidor para el estado de sync** (`getHealthSyncView`) | El lint `react-hooks/purity` prohíbe `Date.now()` en el render de un componente; el «hace X» (formatDistanceToNow + Date.now) se calcula en la query (no-componente) y el RSC solo pinta strings. |
| 2026-07-10 | **AC Fase 3 verificados en navegador con datos reales del PoC** (cookie de sesión sellada, DECISIONS #42): días de Carga (9-10 jul) EXCLUIDOS de la pendiente (1/8 pesajes válidos) y de la adherencia (normalN=1); export 12 tablas/3 días/41 entradas; import CSV preview 4 métricas + kJ/mL + «2 días machacan manual»; ingest 401 sin token; popover F6.6 abre | Verificación end-to-end contra Neon; 0 errores de consola. Cifra 31 filas/10 métricas del AC pende del CSV real de Alex (test conditional `hae-real.csv` listo). |
| 2026-07-10 | **AC F2 · Test de consistencia Gemini** ("200 ml café con leche desnatada" ×3, `gemini-3.1-flash-lite`, thinking low, `Output.object`): **kcal 70/70/70, P 6,4/6,4/6,4, G 0,4/0,4/0,4**; solo C bailó 0,4 g una vez. ~1,4 s/llamada | Con salida estructurada la consistencia es aún mejor que con parseo manual. La cifra que manda (kcal) es idéntica; el ruido sub-gramo lo absorbe la calibración por peso (principio 2). NO es "variación notable". Palanca extra: `thinkingLevel:"minimal"`. |
